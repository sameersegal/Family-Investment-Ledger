---
description: Query Neo Ledger config, entities, securities, schema, or prepare validation and ingest workflows
argument-hint: "[config|entities|securities|schema|validate|ingest]"
---

Load the `family-ledger` skill and handle the requested Neo Ledger API action.

If an argument is provided, use it to route the request.
If no argument is provided, show a short help summary covering `config`, `entities`, `securities`, `schema`, `validate`, and `ingest`.